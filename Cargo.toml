[package]
name = "raknet-rs"
version = "0.1.0"
edition = "2024"
authors = ["Antigravity & open.mp developers"]
description = "Modern Rust FFI wrapper for modified RakNet 2.52 used in open.mp"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
